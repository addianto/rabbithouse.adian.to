[project]
name = "rabbithouse"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = [
    "ansible-core==2.21.1",
]

[dependency-groups]
dev = [
    "ansible-dev-tools>=26.6.1",
    "pre-commit>=4.6.0",
]
